package controller;

/**
 * The enum class of mode which is used in controller.
 *
 * @author rasielchang
 */
public enum ModeConstants {

    SELECTION_MODE,
    ADD_MODE,
    CONNECT_MODE,
    FISHEYE_MODE
            
}